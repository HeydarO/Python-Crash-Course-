#adding number to the list using range() function:
numbers = list(range(1,6))
print(numbers)

#adding only even numbers till 10 using list() and range() functions:
numbers = list(range(2,11,2))
print(numbers)
